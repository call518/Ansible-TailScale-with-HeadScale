---
- name: Restart headscale
  ansible.builtin.systemd_service:
    name: headscale
    state: restarted
    daemon_reload: true

- name: Reload firewalld
  ansible.builtin.service:
    name: firewalld
    state: reloaded
